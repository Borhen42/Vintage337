/**
 * Production (`ng build`). Set `apiBaseUrl` to your public API origin (no trailing slash).
 * Or use `npm run build:deploy` with env `VINTAGE337_API_BASE_URL` (see DEPLOY-VPS.md).
 */
export const environment = {
  production: true,
  apiBaseUrl: '',
};
