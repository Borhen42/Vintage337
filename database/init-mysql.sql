-- Legacy MySQL bootstrap (no longer used — the app uses PostgreSQL / Supabase).
-- See database/supabase-notes.txt for the current database setup.

CREATE DATABASE IF NOT EXISTS vintage337_db
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
